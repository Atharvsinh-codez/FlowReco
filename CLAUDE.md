# FlowReco contributor entry point

Read `AGENTS.md` before making changes. It is the source of truth for generated-file
boundaries, checks, FlowReco product language, and the internal `@cap/*`/`cap-*`
compatibility names that must not be renamed without a migration plan.
